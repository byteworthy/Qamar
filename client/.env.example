# =============================================================================
# NOOR CBT CLIENT - Environment Configuration Template
# =============================================================================
# Copy this file to client/.env for local development.
# Expo reads EXPO_PUBLIC_ prefixed variables at build time.
# =============================================================================

# -----------------------------------------------------------------------------
# VALIDATION MODE (Client)
# -----------------------------------------------------------------------------
# When NOT set to "false", client runs in validation mode:
# - Paid tiers show "Coming Soon" badges
# - Purchase buttons are disabled
# - Suitable for tester builds without real billing
#
# Set to "false" only when ready for production with real IAP.
EXPO_PUBLIC_VALIDATION_MODE=true

# -----------------------------------------------------------------------------
# API DOMAIN
# -----------------------------------------------------------------------------
# The domain where the backend server is running.
# For local development: localhost:5000
# For Replit: your-app.repl.co
# For production: api.yourdomain.com
EXPO_PUBLIC_DOMAIN=localhost:5000
